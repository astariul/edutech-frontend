import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
  playIcon: {
    display: "flex",
    marginLeft: "auto",
    marginRight: "auto"
  }
}));

export default useStyles;
