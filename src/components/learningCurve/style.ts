import { createStyles } from "@mantine/core";

const useStyles = createStyles(() => ({
  chart: {
    margin: "20px auto",
    backgroundColor: "#fff",
    paddingTop: "20px",
    border: "1px solid #ddd",
    boxShadow: "0 22px 35px -16px rgb(0 0 0 / 10%)",
  },

}));

export default useStyles;
