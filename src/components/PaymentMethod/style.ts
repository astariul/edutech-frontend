import { createStyles } from "@mantine/core";
const useStyles = createStyles(() => ({
  cart: {
    width: "100%",
    marginRight: "32px",
    marginBottom: "120px",
  },
  container: {
    display: "flex",
    flexDirection: "row",
  },
}));

export default useStyles;
